# 项目名称
project_name: xxx项目名称

# 执行人
tester_name: 陈鑫

# 实时更新用例内容，False时，已生成的代码不会在做变更
# 设置为True的时候，修改yaml文件的用例，代码中的内容会实时更新
real_time_update_test_cases: False

host: https://www.wanandroid.com


# 登录信息，debugtalk.py用到(老的登录配置信息, 换成新的配置以后可以去掉)
login_config:
  user_name: 大君2259
  user_pass: 123456


# 接口录制存储路径
record_info:
  # 需要录制url。如：https://www.wanandroid.com
  host:
  # 监听地址
  listen_host: '127.0.0.1'
  # 监听端口
  listen_port: 8889
  # 存储的路径地址。一般一个模块一个目录
  path: yanshi

# 批量修改录制下来的接口请求信息路径
update_path: yanshi

# 邮箱信息
email:
  # 发件人邮箱
  send_user:
  email_host:
  # 授权码
  stamp_key:
  # 收件人邮箱
  send_list:

# 钉钉机器人信息
ding_talk:
  webhook: 钉钉群创建机器人获取
  secret: 钉钉群创建机器人获取

# 数据库信息
mysql_db:
  host:
  user:
  password:
  database:
  port:

